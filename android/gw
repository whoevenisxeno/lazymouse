#!/usr/bin/env bash
# gradlew shim: the stock gradlew script mangles DEFAULT_JVM_OPTS on this system,
# so invoke the wrapper main class directly.
cd "$(dirname "$0")"
exec /usr/lib/jvm/java-21-openjdk/bin/java \
  -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain "$@"
